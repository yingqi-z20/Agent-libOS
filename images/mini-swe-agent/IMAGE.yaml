image_id: mini-swe-agent:v0
name: mini-swe-agent
version: v0
prompt: prompt.md
prompt_mode: image_only
jit_tool_exposure: direct
context_policy: recency_first
safety_profile: mini-swe-agent
jit_tools: tools/jit-tools.json
default_tools: []
# Declarations only: missing entries are diagnosed but do not block boot, and
# these entries never grant the spawned process live authority. The filesystem
# declaration also does not confine direct I/O by the authorized Bash child.
required_capabilities:
  - resource: filesystem:workspace:*
    rights: [read, write]
  - resource: shell:*
    rights: [execute]
metadata:
  inspired_by_project: SWE-agent/mini-swe-agent
  inspired_by_config_path: src/minisweagent/config/mini.yaml
  compatibility_scope: inspiration_only_single_bash_tool_shape
